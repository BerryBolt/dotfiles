# OpenClaw secrets (DO NOT COMMIT)
# Values injected from chezmoi config during bootstrap.

# 1Password service account token (required for non-interactive access)
OP_SERVICE_ACCOUNT_TOKEN="{{ .op_service_account_token }}"

# Default vault name
OP_VAULT="{{ .op_vault }}"