# Bootstrap tools added by the dotfiles repo. Omarchy supplies Git, OpenSSH,
# and mise itself; other entries in ~/.config/mise/config.toml stay user-owned.
# install.sh installs the same list before the first apply.

[tools]
chezmoi = "latest"
1password-cli = "latest"
